<h1><%= @wiki.title %></h1>
<h4><%= markdown(@wiki.body) %></h4>
<small>Created by: <%= @wiki.user.username %> at: <%= @wiki.created_at %></small>
<div>
  <%= link_to "Delete Wiki", @topic, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this wiki?' } %>
  <%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-success' %>
</div>
